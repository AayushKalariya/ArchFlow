Read `AGENTS.md` before starting

What we are going to be doing is adding the design system and UI primitive compoenents

Install and configure `shadcn/ui`.

Add the appropriate shadcn componenets needed for this project.

Also Install `lucide-react`.

Makre sure all the components match the existing dark theme in `globals.css`. Do not use any color componenets that are light.

